# NonMouse - Camera Mouse Controller
# Core dependencies

# Hand tracking
mediapipe>=0.8.10

# Computer vision
opencv-contrib-python>=4.5
opencv-python>=4.5

# Math
numpy>=1.19

# Input control
keyboard>=0.13
pynput>=1.6

# Image processing (for gesture guide UI)
Pillow>=9.0

# GUI (matplotlib optional, for plotting)
matplotlib>=3.5

# Packaging
pyinstaller>=4.6

# macOS specific
pyobjc>=7.3; sys_platform == 'darwin'
pyobjc-core>=7.3; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=7.3; sys_platform == 'darwin'
pyobjc-framework-Quartz>=7.3; sys_platform == 'darwin'
